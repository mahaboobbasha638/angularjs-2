import { AttributeStyleDirective } from './attribute-style.directive';

describe('AttributeStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeStyleDirective();
    expect(directive).toBeTruthy();
  });
});
